{
  "name": "husky-pre-commit-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "prettier --check .",
    "husky:pre-commit:prettier:format": "prettier --cache --loglevel warn --write 'src/**/*.{js,scss,css}'",
    "prettier:format": "prettier --cache --write 'src/**/*.{js,css,scss}'",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.3",
    "prettier": "^2.8.7",
    "sass": "^1.62.0"
  }
}
